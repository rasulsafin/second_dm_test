namespace Brio.Docs.Common.Dtos
{
    public enum DynamicFieldType
    {
        OBJECT,
        STRING,
        BOOL,
        INTEGER,
        FLOAT,
        ENUM,
        DATE,
        UNDEFINED,
    }
}
